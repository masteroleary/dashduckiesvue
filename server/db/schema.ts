// Drizzle schema for Postgres.
//
// Phase 2 will port the 7 tables from the old Blazor/EF Core app:
//   User, Duck, DuckSighting, DuckLike, VerificationCode, AuthToken, EmailSubscription
// (GUID -> uuid, DateTime -> timestamptz, soft-delete via deletedAt).
//
// Left intentionally empty for the Phase 1 skeleton.

export {}
